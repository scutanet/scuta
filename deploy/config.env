# Regional VPS fleet — sourced by deploy/common.sh
# Do not commit private keys. Paths are relative to the deploy/ folder
# unless absolute.

DEPLOY_USER=ubuntu
DEPLOY_PATH=/opt/scuta.io
DEPLOY_BRANCH=main

# Public hostnames (health / WSS in production)
NA_HOST=na.scuta.io
EU_HOST=eu.scuta.io
ASIA_HOST=asia.scuta.io

# SSH targets
NA_IP=34.204.42.22
EU_IP=34.248.252.43
ASIA_IP=13.214.137.6

# SSH private keys (relative to deploy/ or absolute paths)
NA_KEY=scuta-key.pem
EU_KEY=scuta-key-eu.pem
ASIA_KEY=scuta-key-asia.pem
